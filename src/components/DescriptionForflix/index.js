import styled from 'styled-components';

const DescriptionForflix = styled.p`
  font-size: 16rem;
  color: var(--color-gray-light);
  font-weight: 300;
  margin-top: 40rem;
`;

export default DescriptionForflix;
