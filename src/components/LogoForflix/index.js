import styled from 'styled-components';
import logoflix from '../../assets/img/Logo.svg';

const LogoForflix = styled.img.attrs({src: logoflix, alt: 'Logo do forflix'}) `
    height: 30px;
`;

export default LogoForflix;