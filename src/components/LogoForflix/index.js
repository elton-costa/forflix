import styled from 'styled-components';
import logo from '../../assets/img/Logo.svg';

const LogoForflix = styled.img.attrs({src: logo, alt: 'Logo do forflix'}) `
    height: 40px;
`;

export default LogoForflix;